[project]
name = "march"
version = "0.0.0"
description = "Arch Linux setup notebook + toolbox (docs tooling)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "mkdocs>=1.6",
  "mkdocs-material>=9.5",
]

[tool.uv]
package = false
